# Unsupported Foundation claims — rejected 2026-09-08

Trạng thái: `REJECTED / EXCLUDED FROM NORMAL INSPIRATION`.

Tác giả duyệt AQ-04 và loại các claim dưới đây khỏi continuity hiện hành sau khi SQLite probe không tìm được proposition trực tiếp đủ mạnh. Việc các từ riêng lẻ có thể xuất hiện trong nguồn không đủ để phục hồi quan hệ hoặc cơ chế.

| Claim family | Nguồn cũ | Kết quả kiểm tra | Trạng thái |
| --- | --- | --- | --- |
| Trụ Thần Thạch là mạng lưới gắn với Du Long Giác | Foundation/ledger/timeline cũ | Không có literal `Trụ Thần Thạch` trong các field SQLite đã khai báo | `REJECTED` |
| Du Long Giác vận hành bằng từ trường/bức xạ | Foundation/ledger cũ | Có hit từ riêng lẻ nhưng chưa có proposition nối với Du Long Giác | `REJECTED` |
| Du Long Giác là vật trấn quốc/trấn yểm | Foundation/timeline cũ | Không có hit cho hai cụm trong các field đã khai báo | `REJECTED` |
| Kế “mượn đao đào ngọc” / “dẫn xà xuất động” | Foundation/chapter prototype cũ | Không có hit cho hai cụm trong các field đã khai báo | `REJECTED` |
| Bản đồ tọa độ toàn quốc, hộp chì phong tỏa, hệ thống đập–mật đạo–hầm binh khí suy từ các cơ chế trên | Dossier/scene cũ | Phụ thuộc claim đã bị bác bỏ; chưa có provenance proposition-level riêng | `REJECTED` |
| Huyết thống hoàng tộc Đoàn thị/Nam Chiếu của Tiêu Phùng | Foundation cũ | Xung đột profile Task 450 được Tác giả chọn | `REJECTED` |

## Quy tắc tái xét

Không agent nào được dùng lại các claim này làm fact, bridge, foreshadowing hay lời giải bí mật chỉ vì chúng có trong Git history, `chapters/archive/` hoặc `archive/pre_restructure_2026_09/`.

Muốn mở lại phải có đủ:

1. query SQLite/XML và kết quả trích xuất chứng minh đúng proposition;
2. locator/provenance ổn định;
3. phân loại `GAME FACT`, `SOURCE-SUPPORTED INFERENCE` hoặc proposal Bridge;
4. phê duyệt riêng của Tác giả.
